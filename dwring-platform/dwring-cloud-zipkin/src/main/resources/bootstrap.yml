server:
  port: 9411
  compression:
    # 是否开启压缩 默认false
    enabled: true
spring:
  application:
    name: zipkin-server
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
management:
   metrics: 
      web: 
         server: 
            auto-time-requests: false